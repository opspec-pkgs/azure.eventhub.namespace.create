name: github.com/opspec-pkgs/azure.eventhubnamespace.create
description: creates an azure event hub namespace (if doesn't already exist)
inputs:
  subscriptionId:
    string:
      constraints: { minLength: 1 }
      description: id of azure subscription
  azureUsername:
    string:
      constraints: { format: email }
      description: email address of azure account
  azurePassword:
    string:
      constraints: { minLength: 1 }
      description: password of azure account
      isSecret: true
  name:
    string:
      constraints: { minLength: 1 }
      description: name of the event hub namespace
  resourceGroup:
    string:
      constraints: { minLength: 1 }
      description: name of azure resource group
  location:
    string:
      constraints: { minLength: 1 }
      description: location of provisioned event hub namespace
      default: westus
  sku:
    string:
      constraints: { enum: [ Basic, Standard ] }
      description: The messaging tier for event hub namespace
      default: Standard
  messagingUnits:
    number:
      constraints: { minimum: 1, maximum: 20 }
      description: the throughput units, 1-20
      default: 1
  template:
    file:
      default: /template.json
run:
  serial:
    - container:
        image: { ref: 'alpine' }
        cmd: [ /cmd.sh ]
        files: { /cmd.sh, /parametersTemplate.json, /parameters.json: parameters }
        envVars: { name, location, sku, messagingUnits }
    - op:
        pkg: { ref: github.com/opspec-pkgs/azure.group.deployment.create#0.0.2 }
        inputs: 
          subscriptionId:
          username: azureUsername
          password: azurePassword
          resourceGroup:
          name:
          parameters:
          template: